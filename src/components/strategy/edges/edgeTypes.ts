
import { createEdgeTypes } from './edgeTypes';
import { EdgeTypes } from '@xyflow/react';

// Export the type of the object returned by createEdgeTypes function
export type EdgeTypesObj = EdgeTypes;

export { createEdgeTypes };
